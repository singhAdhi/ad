//Write a function that takes an integer minutes and converts it to seconds.
// 1 minute = 60 seconds
function minToSec(min){
    const convert=min*60;
    console.log (`the integer to seconds converter takes ${min} and gives output as ${convert}`);
}
minToSec(8);
